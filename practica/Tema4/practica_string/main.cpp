#include <iostream>
#include <string>

using namespace std;

int main()
{
	// const char *arrCopia = "hola";
	// cout << strlen(arrCopia);
	// for (int i = 0; i < strlen(arrCopia); i++)
	// {
	// 	cout << arrCopia[i] << " ";
	// }
	// cout << endl;
	// int size = strlen(arrCopia);
	// //tengo que reservar el doble de memoria o lo hace por defecto c++ al guardar un string en un char array
	// char *_arr = (char *)malloc(size * sizeof(char) * 2);
	// strcpy(_arr, arrCopia);
	// //que termine en /0 es una precondicion o lo tengo que hacer yo
	// _arr[size + 1] = '/0';
	// for (int i = 0; i < strlen(_arr); i++)
	// {
	// 	cout << _arr[i] << " ";
	// }
	// return 0;
}
